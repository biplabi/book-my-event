spring:
  application:
    name: event-service

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/event_db
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update